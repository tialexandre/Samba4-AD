#Installing
#Install Rocky Minimal
#Make sure you point the DNS to Active Directory Server.
#After the GUI install: 
#(Just copy and paste the following line on the Rocky terminal or remote SSH terminal- remote suggested)

dnf -y install wget && cd /root &&  bash <(wget -qO- https://raw.githubusercontent.com/fumatchu/FR-RADS/main/FR-Installer.sh)
